//Declaration
var foo = 'basic string declaration';
var bar = "Can use double and single quotes interchangeably - but use single";
var baz = 'if using single quotes can embed "double quotes" ,and vice versa';

//Concatenation
var baseString = 'This is a some';
var concatenated = baseString + 'text';
var concatenatedWithNumber = baseString + 'text concatenated with a number ' + 42;

console.log(foo);
console.log(bar);
console.log(baz);
console.log(concatenated);
console.log(concatenatedWithNumber);

